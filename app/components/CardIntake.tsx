'use client';
import {useState} from 'react';
export function CardIntake({inventoryId,onDone}:{inventoryId:string;onDone?:()=>void}){const[msg,setMsg]=useState('');async function save(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget,f=new FormData(form);const body={inventoryId,sport:String(f.get('sport')||''),player:String(f.get('player')||''),year:String(f.get('year')||''),setName:String(f.get('setName')||''),cardNumber:String(f.get('cardNumber')||''),parallel:String(f.get('parallel')||''),rookie:f.get('rookie')==='on',insertName:String(f.get('insertName')||''),gradingCompany:String(f.get('gradingCompany')||''),grade:String(f.get('grade')||''),notes:String(f.get('notes')||'')};const r=await fetch('/api/cards',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});if(r.status===401){location.href='/login';return}setMsg(r.ok?'Card details saved':'Could not save card details');if(r.ok)onDone?.()}return <form onSubmit={save}><div className="two"><label>Sport<input name="sport"/></label><label>Player / character<input name="player"/></label></div><div className="four"><label>Year<input name="year"/></label><label>Set<input name="setName"/></label><label>Card #<input name="cardNumber"/></label><label>Parallel<input name="parallel"/></label></div><div className="two"><label>Insert<input name="insertName"/></label><label>Rookie <input name="rookie" type="checkbox"/></label></div><div className="two"><label>Grading company<input name="gradingCompany"/></label><label>Grade<input name="grade"/></label></div><label>Card notes<textarea name="notes"/></label><button className="primary wide">SAVE CARD DETAILS</button>{msg&&<p>{msg}</p>}</form>}