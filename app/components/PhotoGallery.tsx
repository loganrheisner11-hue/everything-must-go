'use client';import {useEffect,useState} from 'react';
type P={id:string;inventory_id:string;url:string|null;is_primary:boolean};
export function PhotoGallery({inventoryId,refresh=0}:{inventoryId:string;refresh?:number}){const[photos,setPhotos]=useState<P[]>([]);async function load(){const r=await fetch('/api/photo-library',{cache:'no-store'});if(r.status===401){location.href='/login';return}if(r.ok){const all:P[]=await r.json();setPhotos(all.filter(p=>p.inventory_id===inventoryId))}}useEffect(()=>{load()},[inventoryId,refresh]);async function primary(p:P){await fetch('/api/photo-library',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({id:p.id,inventoryId,isPrimary:true})});load()}if(!photos.length)return null;return <div style={{display:'grid',gridTemplateColumns:'repeat(auto-fill,minmax(110px,1fr))',gap:10,margin:'14px 0'}}>{photos.map(p=><button key={p.id} type="button" onClick={()=>primary(p)} style={{padding:6,border:p.is_primary?'2px solid currentColor':'1px solid #333',background:'transparent'}}>{p.url&&<img src={p.url} alt="" style={{width:'100%',aspectRatio:'1',objectFit:'cover',display:'block'}}/>}<small>{p.is_primary?'PRIMARY':'Make primary'}</small></button>)}</div>}