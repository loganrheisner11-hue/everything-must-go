'use client';
import {useState} from 'react';import {createClient} from '../../lib/supabase/client';
export default function Login(){const[email,setEmail]=useState('');const[msg,setMsg]=useState('');async function go(e:React.FormEvent){e.preventDefault();setMsg('Sending secure sign-in link…');const supabase=createClient();const {error}=await supabase.auth.signInWithOtp({email,options:{emailRedirectTo:`${location.origin}/auth/callback`}});setMsg(error?error.message:'Check your email for the EMG sign-in link.')}return <main><section className="panel" style={{maxWidth:520,margin:'12vh auto',padding:28}}><span className="eyebrow">EMG // SECURE ACCESS</span><h1>Everything Must Go</h1><p>Sign in to your private inventory command center.</p><form onSubmit={go}><label>Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)} autoComplete="email"/></label><button className="primary wide">EMAIL ME A SIGN-IN LINK</button>{msg&&<p>{msg}</p>}</form></section></main>}
